apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-classifier.fullname" . }}
  labels:
    {{- include "iris-classifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iris-classifier.selectorLabels" . | nindent 4 }}
